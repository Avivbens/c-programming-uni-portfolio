{
    "tasks": [
        {
            "type": "shell",
            "label": "Nx build",
            "command": "${workspaceFolder}/tools/scripts/debug-project.js ${file}",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}
